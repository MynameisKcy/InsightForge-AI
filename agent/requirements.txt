# InsightForge AI — Multi-Agent Data Analysis System
# Python 3.10+  |  推荐使用 conda 环境 AnalysisAgent
# 安装: pip install -r requirements.txt

# ── AI 框架 ──
langchain==1.3.14
langchain-core==1.4.9
langchain-community==0.4.2
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
langgraph==1.2.9

# ── LLM / 向量 / 分词（DashScope 通义千问 + ChromaDB + jieba）──
dashscope==1.26.3
chromadb==1.5.9
jieba==0.42.1

# ── Web 服务（FastAPI SSE 流式 + Streamlit + 文件上传）──
fastapi==0.139.2
uvicorn==0.51.0
python-multipart==0.0.32
streamlit==1.59.2
websockets==15.0.1

# ── 数据处理 / 数据库 ──
pandas==2.3.3
numpy==2.2.6
duckdb==1.5.4
SQLAlchemy==2.0.51

# ── 可视化 ──
plotly==6.9.0

# ── 报告导出 ──
python-docx==1.2.0
reportlab==5.0.0
Jinja2==3.1.6

# ── 配置 / 安全 ──
PyYAML==6.0.3
python-dotenv==1.2.2
bcrypt==5.0.0
